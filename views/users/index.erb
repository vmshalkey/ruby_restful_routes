<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>All Users</title>
</head>
<body>
	<h1>All Users</h1>
	<% @users.each do |u| %>
		<div>
			<h2><a href="/users/<%= u.id %>"><%= u.first_name %> <%= u.last_name %></h2></a>
		</div>
	<% end %>
	<a href="/users/new">Add New User</a>
</body>
</html>